// Initialize the number to check
let number = 7; // You can change this number to test other values

// Check if the number is even or odd using the ternary operator
let result = (number % 2 === 0) ? "Even" : "Odd";

// Log the result
console.log(result);
