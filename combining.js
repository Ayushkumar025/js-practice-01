// Initialize variables
let a = 5;
let b = 10;
let c = 15;

// Combine arithmetic, comparison, logical, and assignment operators in a single expression
let result = (a += 5) > 8 && (b *= 2) < 25 || (c /= 5) === 3;

// Log the result of the expression
console.log("Result: " + result);

// Log the updated values of a, b, and c after the operations
console.log("Updated values: a = " + a + ", b = " + b + ", c = " + c);
