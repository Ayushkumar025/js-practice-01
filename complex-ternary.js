// Initialize a number n
let n = -5;  // You can change this value to test other cases

// Use nested ternary operators to evaluate the number
let result = (n > 0) ? "Positive" : (n < 0) ? "Negative" : "Zero";

// Log the result
console.log(result);
